jQuery(document).ready(function() {

	$(".modal").on('shown', function() {
		//$(this).find(".modal-footer").find(".btn").focus();
		 
		$(this).find(".modal-title-anchor").focus();
        	
        
        
	});
});
